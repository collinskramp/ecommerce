# SERVER ENVIRONMENT CONFIGURATION
# Copy this to /var/www/nodeApps/ecommerce/ecommerce/backend/.env on your server

# Server Configuration
PORT=5001

# Database Configuration - NO AUTHENTICATION (for server)
DB_URL=mongodb://127.0.0.1:27017/ec

# JWT Secret (Use a strong secret in production!)
SECRET=ecommerce_super_secret_jwt_key_2025_change_in_production

# Cloudinary Configuration (for image uploads)
cloud_name=your_cloudinary_cloud_name
api_key=your_cloudinary_api_key
api_secret=your_cloudinary_api_secret

# Stripe Configuration (for payments)
STRIPE_SECRET_KEY=your_stripe_secret_key_here
